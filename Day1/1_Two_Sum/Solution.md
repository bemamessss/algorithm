
--------------------------------------

# 📚 Coding Test Study - Day 1

## 📋 오늘의 문제: [LeetCode #1. Two Sum](https://leetcode.com/problems/two-sum/)

### 1. 문제 핵심 요약

* **목표**: 정수 배열(`nums`)에서 두 수를 더해 목표값(`target`)을 만들 수 있는 두 수의 **인덱스**를 반환.
* **조건**:
* 정답은 반드시 하나만 존재함.
* 같은 요소를 두 번 사용할 수 없음.
* 시간 복잡도  미만으로 해결 권장 (Follow-up).

---

### 2. 알고리즘 포인트: **One-pass Hash Table**

#### ❌ Brute Force (단순 반복)

* 이중 `for`문을 사용하여 모든 쌍을 검사.
* 시간 복잡도: 
* 효율성: 데이터가 많아질수록 성능이 기하급수적으로 저하됨.

#### ✅ Hash Table (해시맵 활용) - **오늘의 핵심**

* 배열을 한 번 순회하면서 **"내가 본 숫자와 그 위치"**를 메모(Map)함.
* **로직**:
1. 현재 숫자 `nums[i]`를 확인.
2. 필요한 값(`target - nums[i]`)이 이미 맵에 있는지 확인 (`containsKey`).
3. 있다면 정답 반환, 없다면 현재 숫자와 인덱스를 맵에 기록 (`put`).


* 시간 복잡도: **** (배열을 단 한 번만 훑음)

---

### 3. Java 문법 & 활용 포인트

#### 🔹 Java Collections: `HashMap`

```java
Map<Integer, Integer> map = new HashMap<>();

```

* **`Map<Integer, Integer>`**: 인터페이스 선언. 제네릭(`<>`)을 사용하여 Key와 Value의 타입을 지정함 (Primitive 타입인 `int` 대신 Wrapper 클래스인 `Integer` 사용).
* **`new HashMap<>()`**: 실제 객체 생성. 뒤쪽의 `<>`는 앞의 타입을 추론하므로 생략 가능(다이아몬드 연산자). `()`는 생성자 호출을 의미함.

#### 🔹 주요 메서드

* **`map.put(key, value)`**: 데이터를 맵에 저장.
* *이번 문제에선 `key`에 숫자 값을, `value`에 인덱스를 저장하여 검색 최적화.*


* **`map.containsKey(key)`**: 특정 키가 존재하는지 확인. ($O(1)$의 속도)
* **`map.get(key)`**: 키에 해당하는 값을 가져옴.

#### 🔹 배열 반환

```java
return new int[] { index1, index2 };

```

* 자바에서 결과값을 배열로 즉시 생성하여 반환하는 정석 문법.

---

### 4. 오늘의 깨달음 (Key Takeaways)

1. **검색 속도 향상**: "특정 값을 반복해서 찾아야 한다면 배열 대신 **해시맵**을 먼저 고려하자."
2. **Key-Value 설정**: 내가 **물어볼 정보(숫자)**가 Key가 되고, 내가 **얻고 싶은 정보(인덱스)**가 Value가 되어야 한다.
3. **효율성**: 직장인 코테 준비는 단순 정답이 아니라 **최적의 패턴**을 암기하는 것이 핵심이다.

---

### 5. 제출 코드 (Java)

```java
import java.util.*;

class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> map = new HashMap<>();
        
        for (int i = 0; i < nums.length; i++) {
            int complement = target - nums[i];
            
            if (map.containsKey(complement)) {
                return new int[] { map.get(complement), i };
            }
            
            map.put(nums[i], i);
        }
        return null;
    }
}
