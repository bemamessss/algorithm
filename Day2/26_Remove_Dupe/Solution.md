

# 📚 Coding Test Study - Day 2

## 📋 오늘의 문제: [LeetCode #26. Remove Duplicates from Sorted Array](https://leetcode.com/problems/remove-duplicates-from-sorted-array/)

### 1. 문제 핵심 요약

* **목표**: 정렬된 정수 배열(`nums`)에서 중복된 요소를 제거하여 각 숫자가 **단 한 번만** 나오도록 수정.
* **조건**:
* 새로운 배열을 만들지 않고 **제자리에서(In-place)** 수정할 것.
* 요소의 상대적 순서는 유지해야 함.
* 수정 후 고유한 요소의 개수 를 반환하고, `nums`의 앞부분 개에 정답이 담겨야 함.



---

### 2. 알고리즘 포인트: **Two Pointers (In-place)**

#### ❌ 새로운 배열 생성 (제한 조건 위반)

* 중복을 제거한 새 배열을 만들면 쉽지만, 문제에서 요구하는 **공간 복잡도 **을 만족할 수 없음.

#### ✅ Two Pointers (투 포인터 활용) - **오늘의 핵심**

* 배열이 **이미 정렬**되어 있다는 점을 이용해, 이전 값과 현재 값을 비교하며 나아감.
* **로직**:
1. `insertIndex` (또는 `point`): 고유한 값을 채워 넣을 위치를 관리 (1번 인덱스부터 시작).
2. `i` (탐색 포인터): 배열을 돌며 중복 여부를 확인.
3. **비교**: `nums[i]`가 바로 앞의 값(`nums[i-1]`)과 다르면, 그 값은 새로운 고유값!
4. **기록**: `nums[insertIndex]` 자리에 `nums[i]`를 쓰고 `insertIndex`를 1 증가시킴.


* **시간 복잡도**:  (배열을 한 번만 순회)
* **공간 복잡도**:  (추가 메모리 사용 없음)

---

### 3. Java 문법 & 활용 포인트

#### 🔹 증감 연산자의 활용 (`point++`)

```java
nums[point++] = nums[i];

```

* **후위 증감(`point++`)**: 현재 `point` 위치에 값을 먼저 대입한 후, `point` 값을 1 증가시킴.
* 위 코드는 `nums[point] = nums[i]; point = point + 1;`과 동일한 의미를 가짐.
* 코드를 간결하게 만들지만, 실행 순서(선 대입 후 증가)를 명확히 이해해야 함.

#### 🔹 예외 처리 (Edge Case)

* 배열의 길이가 0인 경우, 루프를 돌기 전에 `0`을 반환하는 예외 처리가 안전함.
* `if (nums.length == 0) return 0;`

---

### 4. 오늘의 깨달음 (Key Takeaways)

1. **정렬된 배열의 특징**: "정렬되어 있다면 중복된 값은 반드시 **서로 이웃**해 있다."
2. **In-place의 의미**: "새로운 공간을 쓰지 말라는 제약이 있다면 **포인터(인덱스 변수)**를 활용해 기존 배열을 덮어씌우자."
3. **포인터의 시작점**: 0번 인덱스는 비교할 대상이 없으므로 항상 고유함. 따라서 탐색과 기록은 **1번 인덱스**부터 시작하면 효율적임.

---

### 5. 제출 코드 (Java)

```java
class Solution {
    public int removeDuplicates(int[] nums) {
        // 엣지 케이스 처리
        if (nums.length == 0) return 0;

        // point: 다음에 고유값이 들어갈 위치
        int point = 1; 
        
        for (int i = 1; i < nums.length; i++) {
            // 현재 값과 이전 값이 다르면 고유한 값 발견!
            if (nums[i] != nums[i - 1]) {
                nums[point] = nums[i];
                point++;
            }
        }
        
        // 고유한 요소의 개수 k 반환
        return point;
    }
}
